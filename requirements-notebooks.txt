# Extra dependencies for running the notebooks/ walkthroughs locally.
# Not needed for the default CLI pipeline, which only requires requirements.txt.
-r requirements.txt
jupyter>=1.0,<2
ipykernel>=6.31,<7
nbformat>=5.10,<6
